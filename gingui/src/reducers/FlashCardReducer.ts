const defaultState: FlashCardState = {flashcards: []};

export default (state: FlashCardState = defaultState, action: any) => {
    switch (action.type) {
        default:
            return state;
    }
}

